<?xml version="1.0" encoding="UTF-16"?>
<!--
  Paperboy Watcher — Windows Task Scheduler config

  HOW TO USE:
  1. Replace C:\path\to\npx.cmd with your npx path (run: where npx)
  2. Import: schtasks /create /tn "PaperboyWatcher" /xml "path\to\this\file.xml"

  Or use the one-liner:
    schtasks /create /tn "PaperboyWatcher" /tr "\"C:\path\to\npx.cmd\" paperboy watch" /sc onlogon /rl limited

  If installed globally (npm install -g paperboy):
    schtasks /create /tn "PaperboyWatcher" /tr "\"C:\path\to\paperboy.cmd\" watch" /sc onlogon /rl limited
-->
<Task version="1.2" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <Triggers>
    <LogonTrigger>
      <Enabled>true</Enabled>
    </LogonTrigger>
  </Triggers>
  <Settings>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <DisallowStartIfOnBatteries>false</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>false</StopIfGoingOnBatteries>
    <ExecutionTimeLimit>PT0S</ExecutionTimeLimit>
    <RestartOnFailure>
      <Interval>PT1M</Interval>
      <Count>3</Count>
    </RestartOnFailure>
  </Settings>
  <Actions>
    <Exec>
      <Command>C:\path\to\npx.cmd</Command>
      <Arguments>paperboy watch</Arguments>
    </Exec>
  </Actions>
</Task>
