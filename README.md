# send-to-kindle

MCP server that receives markdown, converts to EPUB, and sends via email.

**MVP:** Learning-focused implementation of core functionality.
**Future:** Convert to CLI tool for broader use.

## Quick Start

```bash
npm install
npm run dev
```

## MVP Scope

- Receive markdown via MCP
- Convert to EPUB format
- Send via email
